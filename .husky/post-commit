#!/usr/bin/env sh
# Auto re-index GitNexus after commit (background, non-blocking).
# Skips if .gitnexus/ not initialized or if last re-index was < 60s ago (throttle).

[ ! -d .gitnexus ] && exit 0
[ ! -x ./scripts/plane ] && exit 0

./scripts/plane gitnexus reindex-bg
